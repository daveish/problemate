// import { fromJS } from 'immutable';
// import { makeSelectAppDrawerDomain } from '../selectors';

// const selector = makeSelectAppDrawerDomain();

describe('makeSelectAppDrawerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

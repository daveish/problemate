// import { fromJS } from 'immutable';
// import { makeSelectAppNavigationDomain } from '../selectors';

// const selector = makeSelectAppNavigationDomain();

describe('makeSelectAppNavigationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
